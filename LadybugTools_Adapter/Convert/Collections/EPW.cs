using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace BH.Adapter.LadybugTools
{
    public static partial class Convert
    {
        /*public static BH.oM.LadybugTools.EPW EPW(BH.Adapter.LadybugTools.EPW oldObject)
        {
            return new oM.LadybugTools.EPW()
            {
                Location = Location(oldObject.Location),
                DataCollections = oldObject.DataCollections.Select(x => ToHourlyContinuousCollection(x)).ToList(),
                Metadata = oldObject.Metadata
            };
        }*/
    }
}
