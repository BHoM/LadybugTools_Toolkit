# TODO - re-add method in here - it got lost somewhere, but it just something to plot a typical day's variable values like the UTCI composite plot, and possibly return value as a dataframe

"""
Also, use kwargs cos they're cool

f = representative_day(
    epw,
    variable=var,
    other_days=other_days,
    month=month,
    title=f"{epw_name}",
    show_min=False,
    show_max=True if month == 8 else False,
    show_mean=True,
    show_typical=True,
    all_color="#898786",
    all_alpha=0.1,
    typical_color="#006B44",
    mean_color="k",
    max_color="#E0403F",
    highlight_other_days=other_days,
    other_color="orange"
)
"""
